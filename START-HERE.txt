Extract this ZIP. In the GitHub repository, open public/assets/ and upload app.js and styles.css from this package, replacing the existing files. Then open public/ and upload index.html, replacing it. Commit the three replacements, wait for the new Actions run to turn green, and refresh the COBRA statistics page. Wheel gestures inside the map now zoom; the normal desktop map is shorter so the dialog fits the screen.
