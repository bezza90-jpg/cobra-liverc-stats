The House of Sport label now sits behind car markers on the driver distance tracker.

GitHub: extract this ZIP, then upload its public folder at the repository TOP LEVEL (Add file > Upload files); check public/assets/app.js and public/index.html before committing. Wait for the green Actions run, then refresh the page.

Desktop site manager: copy those same two files to the matching public paths in your extracted manager folder too, so a later manager export does not restore an older tracker script. No admin or data files are changed.
