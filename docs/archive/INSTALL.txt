COBRA distance tracker — window scrolling update

Extract this ZIP and replace these four files in your existing cobra-liverc-stats GitHub repository, preserving these exact paths:

public/index.html
public/assets/app.js
public/assets/styles.css
public/assets/matt-hodges-car.png

Leave the other repository files alone. Commit the replacements, wait for the GitHub Action to show success, then reload the Wix page (or use a private tab if the older version remains cached).

The distance tracker now stays within the available window and scrolls its own white information panel instead of scrolling the page behind. On desktop, the mouse wheel over the map scrolls the information; use the map's + and - controls to zoom. This package includes the previous mobile layout, Matt's car avatar, driver following, replay from a chosen slider point, and round-by-round movement.
